import pytest
pytest.skip("Skip standalone scratch HTTP test during pytest", allow_module_level=True)

# This file is a manual scratch script used for ad-hoc testing against a running
# backend. Run manually outside pytest when needed.
